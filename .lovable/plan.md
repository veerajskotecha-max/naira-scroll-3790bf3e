

## Plan: Update Contact Address

Update the `contactCards` array in `src/pages/ContactUs.tsx` to replace the current address lines with the new address.

### Edit `src/pages/ContactUs.tsx`
- Change line `"123 Couture Lane, Bandra West"` → `"Flat 7, Veeraj Blossom, Karanyogi Nagar"`
- Change line `"Mumbai, Maharashtra 400050"` → `"Nashik, Maharashtra 422002"`

